<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.freshfridge">

    <!-- 🔔 สิทธิ์สำหรับใช้ Alarm -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="FreshFridge"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreshFridge">

        <!-- ✅ MainActivity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ➕ AddFoodActivity -->
        <activity android:name=".ui.AddFoodActivity" />

        <!-- ✅ MealPlannerActivity -->
        <activity android:name=".ui.MealPlannerActivity" />

        <!-- ✅ FridgeActivity (เพิ่มตรงนี้!) -->
        <activity android:name=".ui.FridgeActivity" />

        <!-- 🔔 Broadcast Receiver สำหรับแจ้งเตือน -->
        <receiver
            android:name=".receiver.NotificationReceiver"
            android:exported="false" />
    </application>

</manifest>
